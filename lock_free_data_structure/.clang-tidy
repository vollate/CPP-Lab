---
# This is a basic configuration for clang-tidy.
# It enables a selection of checks that are generally useful for modern C++.
# The '-' at the beginning of the list disables all checks by default,
# and then specific check groups are re-enabled.

# Only check headers from the project source directories
HeaderFilterRegex: "^.*/OBCX/(include|src|examples|tests)/.*$"

Checks: >
  -*,
  bugprone-*,
  clang-analyzer-*,
  cppcoreguidelines-*,
  modernize-*,
  performance-*,
  portability-*,
  - readability-*,
  - bugprone-easily-swappable-parameters,
  - cppcoreguidelines-avoid-capturing-lambda-coroutines,
  - cppcoreguidelines-avoid-reference-coroutine-parameters,
  - cppcoreguidelines-non-private-member-variables-in-classes,
CheckOptions:
  # This option configures the 'modernize-use-trailing-return-type' check.
  # By setting 'DefinitionRequired' to '0' (false), we enforce the use of
  # trailing return types for both function declarations and definitions,
  # not just definitions. This helps maintain a consistent function syntax
  # across the entire codebase.
  - key: modernize-use-trailing-return-type.DefinitionRequired
    value: "0"
  - key: cppcoreguidelines-avoid-do-while.IgnoreMacros
    value: "1"
  - key: cppcoreguidelines-avoid-non-const-global-variables.AllowInternalLinkage
    value: true
  - key: readability-magic-numbers.AllowPointerConditions
    value: true
